fastapi==0.115.0          # Web framework để viết API
uvicorn==0.30.6           # Server chạy FastAPI
pydantic==2.9.2           # Dùng để định nghĩa model, validate dữ liệu
pyodbc==5.2.0             # Kết nối SQL Server qua ODBC
python-dotenv==1.0.1      # Đọc biến môi trường (.env)
